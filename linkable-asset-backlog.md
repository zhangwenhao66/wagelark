# WageLark — Linkable Asset Backlog

"可被引用资产"（linkable asset）点子清单。由定时任务 `trafficsite-linkable-asset-planning` 维护，实际制作由 `trafficsite-linkable-asset-building` 负责。

状态标注：`[待制作]` / `[制作中]` / `[已发布]`

站点定位：职业薪资+职业介绍参考站（职业hub/州组合/what-does-X-do/入行路径/最低工资），对标 careerexplorer/salary.com，属于矩阵第一个 YMYL-adjacent 站——只呈现 BLS 公开数据，不给个人职业建议、不做收入承诺式表述、不推荐培训机构。

数据基础（2026-08-17 盘点，制作前请先跑一遍确认没有过期）：`src/data/bls-wages.ts` 目前收录 27 个职业的完整 BLS 结构化数据（`medianAnnual`、`percentiles.p10`/`p90`——27 条里 21 条两个百分位都有、`jobOutlookPct`、`employmentChange`、`entryEducation`、`industryWages`），`src/data/guides.ts` 目前只有 30 篇文章（Salary Guide/How to Become/Career Guide 三类）。**没有任何分州、分经验年限的细分数据**（`工资分位数工具-实施方案_20260804.md` 里已经因为这个原因否决过"地区+经验年限计算器"方案，做州级点子前必须先确认能拿到真实 BLS OES 州级数据，不能编造）。已有一个按分位数定位单个用户输入薪资的工具 `src/components/SalaryPercentileTool.astro`（嵌在每篇 salary-guide 文章页里），下面几个点子均不与它重复。

---

## 1. "免学位高薪职业"排行研究 `[待制作]`

- **类别**：原创数据研究
- **具体做什么**：用 `bls-wages.ts` 里已有的 `entryEducation` 字段（High school diploma / Postsecondary nondegree award / Associate's degree / Bachelor's / Master's / Doctoral 六档）对 27 个职业做交叉分析，按"入行学历门槛"分组后各组内按 `medianAnnual`（及 `percentiles.p90`）排序，产出一篇"不需要四年制学位、薪资中位数最高的 N 个职业"的数据文章，附排行表格（职业、入行学历、中位年薪、p90、职业增长率 `jobOutlookPct`）。全部数据已在仓库现成字段里，不需要新采集。
- **为什么会被引用**：个人理财博主、"gap year/不上大学"类内容创作者、社区大学招生页、职业规划类 subreddit 都在找"高薪不需要学位的工作"这类可核实数据支撑的排行——比同类内容常见的"拍脑袋列举"更有说服力，因为每个数字都能标注 BLS 来源年份可核实。
- **制作复杂度**：低。数据已全部就绪，主要工作是筛选/排序/写作，可选加一张静态排行条形图（学历门槛 vs 中位年薪）。
- **备注**：文案里必须清楚标注"入行最低学历要求"不等于"不需要任何培训"（比如 Postsecondary nondegree award 类仍需数月到一年培训），避免被解读成收入承诺式表述。

## 2. "哪些职业薪资差距最大"分散度研究 `[待制作]`

- **类别**：原创数据研究
- **具体做什么**：用现有 `percentiles.p10`/`p90` 两个百分位（27 个职业里 21 个两者都有，其余 6 个数据不全需要在文中footnote说明排除或标注"数据不全"）计算每个职业的 p90/p10 倍数或 (p90-p10)/medianAnnual 离散度，排出"薪资差距最悬殊"和"薪资最稳定/标准化"两端的职业清单，讨论造成差距的常见因素（地区、雇主类型、经验年限——用 `industryWages` 字段里已有的行业薪资差异做旁证，不编造分经验数据）。
- **为什么会被引用**：这是一个记者/薪酬分析师/求职内容站都会引用的"数据新闻"角度（"这份工作看运气/看公司,那份工作到哪都差不多"），比单纯罗列中位数更有新闻价值，容易被薪酬对比类文章或求职媒体当数据源引用并回链。
- **制作复杂度**：低到中。计算逻辑简单（纯函数+单测，参照 `SalaryPercentileTool` 已有的"纯计算函数+单测"模式），主要工作量在写作和验证 6 个数据不全职业不要被错误纳入排名。
- **备注**：必须在文中明确这不是"哪个职业更好"的判断，只是薪资分布宽窄的客观描述，避免被误读为对某职业的价值判断。

## 3. 职业"薪资 vs 增长率"交互式矩阵图 `[待制作]`

- **类别**：交互式工具
- **具体做什么**：一个交互式散点/象限图，X 轴 `medianAnnual`、Y 轴 `jobOutlookPct`（或 `employmentChange`），把 27 个职业全部点在图上，可按 `entryEducation` 筛选高亮，鼠标悬停显示职业名+具体数字+跳转到对应 salary-guide 文章的链接。技术上延续本站"纯静态 Astro + vanilla `<script type="module">` island，不新增框架依赖"的既有约束（`工资分位数工具-实施方案` 里已定的模式），用轻量 SVG/Canvas 手写即可，不需要引入图表库。额外做一个可被第三方 `<iframe>` 嵌入的独立 embed 路由（参照 CalcBadger 已验证过的嵌入组件模式）。
- **为什么会被引用**："高薪+高增长"象限图是求职/职业规划类内容里最经典的可嵌入可视化资产之一——大学职业中心页面、理财博客的"未来热门职业"文章、招聘平台的行业报告都习惯嵌入这类图并注明数据来源，比纯文字排行更容易被直接引用图片/iframe。
- **制作复杂度**：中。需要写交互图表逻辑+embed 路由+响应式适配，比前两个数据研究点子工作量明显更大，建议放在后面几个点子验证过引用效果后再投入。
- **备注**：图表下方必须显著标注 BLS 数据年份和来源链接（跟站内其他内容一致的口径），避免被当作"过时数据"的可信度问题。

## 4. "27 个职业完整 BLS 数据"终极对照页 `[待制作]`

- **类别**：终极合集页
- **具体做什么**：一个单页的可排序/可筛选大表格，把 `bls-wages.ts` 里 27 个职业的全部字段（中位年薪、p10/p90、职业增长率、入行学历、就业规模 `employment`、行业薪资细分 `industryWages`）一次性并排展示，每行链接回对应的 salary-guide/how-to-become 文章，明确定位为"比任何单篇文章都更全面的一站式参考"，用于断链置换和资源页替换外链战术。
- **为什么会被引用**：职业规划类资源页（社区大学就业指导中心、图书馆职业资源页、"高薪职业清单"类文章）经常链接到 BLS 官网原始数据或聚合表格，但 BLS 官网本身分散在几十个独立页面不便阅读——一个整理好、可排序、标注数据年份的单页对照表是资源页外链战术里"比现有链接目标更好"的典型替换候选。
- **制作复杂度**：低到中。数据 100% 已就绪，主要工作是表格 UI（排序/筛选交互）+ 页面 SEO 结构（面包屑/schema），可以复用 `SalaryPercentileTool` 之外站内已有的表格样式模式。
- **备注**：优先于第 3 项制作，因为静态表格比交互图表落地更快，且是断链置换战术的直接弹药，ROI 更明确。
