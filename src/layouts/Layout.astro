---
import '../styles/global.css';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_TITLE, SITE_DESCRIPTION, ADSENSE_PUBLISHER_ID, GA4_ID, CLARITY_ID } from '../consts';

interface Props {
	title?: string;
	description?: string;
	ogImage?: string;
	ogType?: string;
}

const {
	title = SITE_TITLE,
	description = SITE_DESCRIPTION,
	ogImage = '/favicon.svg',
	ogType = 'website',
} = Astro.props;

const pageTitle = title === SITE_TITLE ? title : `${title} | ${SITE_TITLE}`;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const ogImageURL = new URL(ogImage, Astro.site);
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<meta name="theme-color" content="#f7f6f2" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="canonical" href={canonicalURL} />

		<meta name="google-adsense-account" content={ADSENSE_PUBLISHER_ID} />
		<script
			async
			src={`https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-${ADSENSE_PUBLISHER_ID}`}
			crossorigin="anonymous"></script>

		<script
			async
			src={`https://www.googletagmanager.com/gtag/js?id=${GA4_ID}`}></script>
		<script is:inline define:vars={{ GA4_ID }}>
			window.dataLayer = window.dataLayer || [];
			function gtag() {
				dataLayer.push(arguments);
			}
			gtag('js', new Date());
			gtag('config', GA4_ID);
		</script>

		<script is:inline define:vars={{ CLARITY_ID }}>
			(function (c, l, a, r, i, t, y) {
				c[a] =
					c[a] ||
					function () {
						(c[a].q = c[a].q || []).push(arguments);
					};
				t = l.createElement(r);
				t.async = 1;
				t.src = 'https://www.clarity.ms/tag/' + i + '?ref=bwt';
				y = l.getElementsByTagName(r)[0];
				y.parentNode.insertBefore(t, y);
			})(window, document, 'clarity', 'script', CLARITY_ID);
		</script>

		<title>{pageTitle}</title>
		<meta name="description" content={description} />

		<meta property="og:type" content={ogType} />
		<meta property="og:title" content={pageTitle} />
		<meta property="og:description" content={description} />
		<meta property="og:image" content={ogImageURL} />
		<meta property="og:url" content={canonicalURL} />
		<meta name="twitter:card" content="summary_large_image" />
		<meta name="twitter:title" content={pageTitle} />
		<meta name="twitter:description" content={description} />
		<meta name="twitter:image" content={ogImageURL} />

		<slot name="head" />
	</head>
	<body class="flex min-h-screen flex-col">
		<Header />
		<main class="mx-auto w-full max-w-4xl flex-1 px-4 py-8">
			<slot />
		</main>

		<div class="adsterra-slots">
			<script
				async="async"
				data-cfasync="false"
				src="https://pl31132745.profitableratecpmnetwork.com/30c0f7f4e3e9957f8aad991f190e3df7/invoke.js"></script>
			<div id="container-30c0f7f4e3e9957f8aad991f190e3df7"></div>

			<script is:inline>
				atOptions = {
					'key': '6590e4ff80991a9dc835d5031e2929f1',
					'format': 'iframe',
					'height': 250,
					'width': 300,
					'params': {},
				};
			</script>
			<script is:inline src="https://www.highrevenueformat.com/6590e4ff80991a9dc835d5031e2929f1/invoke.js"></script>

			<div class="adsterra-leaderboard">
				<script is:inline>
					atOptions = {
						'key': '0c370fb174f145b2c95bb216590b60f3',
						'format': 'iframe',
						'height': 90,
						'width': 728,
						'params': {},
					};
				</script>
				<script is:inline src="https://www.highrevenueformat.com/0c370fb174f145b2c95bb216590b60f3/invoke.js"></script>
			</div>
		</div>

		<style>
			.adsterra-slots {
				display: flex;
				flex-direction: column;
				align-items: center;
				gap: 1.5rem;
				max-width: 48rem;
				margin: 0 auto;
				padding: 1.5rem 1rem;
			}
			.adsterra-leaderboard {
				max-width: 100%;
				overflow-x: auto;
			}
		</style>

		<Footer />
	</body>
</html>
