# 内容发布日志（WageLark）

## 2026-08-03 — 建站 + 首批 10 篇文章上线

- **背景**：WageLark（wagelark.com）是流量站矩阵第九个站，美国职业薪资/职业介绍参考站，对标 careerexplorer.com/salary.com，四种页型（Salary Guide / Career Guide / How to Become，含口语化查询归入 Salary Guide）。矩阵第一个 YMYL-adjacent 站——薪资/职业数据会影响真人的职业与培训决策，见建站计划 `独立站/WageLark建站计划_20260803.md` 第四、五节。
- **技术栈**：Astro + Cloudflare Pages，仓库结构照抄 `独立站/mythcairn/`，`vendor/site-toolkit` submodule 固定在 commit `0c549d8`。设计上跟其余内容站区分：navy(`#14273f`)/teal(`#0f8b7f`) 配色 + Inter/Roboto Mono 字体，营造"政府统计手册"而非"编辑部博客"的调性。
- **BLS 数据管线**（本站核心，`tools/bls-data/`）：
  - `wages-source.json`：单一数据源，9 个职业（SOC code 为 key），每条都独立 WebFetch 对应 BLS Occupational Outlook Handbook 页面手工核对（median/percentile/employment/outlook/行业细分工资/entry education），标注 `sourceUrl`/`dataYear`/`publishedDate`
  - `build-wage-data.mjs`：从 `wages-source.json` 生成 `src/data/bls-wages.ts`（带 TypeScript 类型的只读查找表），`guides.ts` 里的文章通过 `socCode` 字段引用，避免同一个数字在正文和数据表里各写一份
  - `wages-source.test.mjs`：`node --test` 单元测试，10 项——9 个职业的字段完整性/百分位单调性/`medianHourly*2080≈medianAnnual`校验，加 5 个职业的硬编码 spot-check（数值独立于生成脚本，直接对照 2026-08-03 从 BLS 官网抄下的数字）
  - `generate-charts.mjs`：从同一份 `wages-source.json` 生成 5 张 SVG 百分位柱状图（`public/images/*-salary-chart.svg`），保证图表数字和正文数字永远来自同一处
- **首日 10 篇配比**（按建站计划第九节）：
  - Salary Guide ×4：dental-hygienist-salary / actuary-salary / pharmacist-salary / physical-therapist-salary
  - Career Guide ×3：what-does-a-physician-assistant-do / what-does-an-actuary-do / what-does-a-paralegal-do
  - How to Become ×2：how-to-become-an-ultrasound-tech / how-to-become-a-phlebotomist
  - 口语化查询 ×1：how-much-do-flight-attendants-make
  - 无州组合页（建站计划第三节红线：2026-08-10 前禁用）
- **独立审核**：3 个并行的全新 Agent（互不知道彼此和写作过程），每个覆盖 3-4 篇，各自独立 WebFetch 对应 BLS 官网页面逐条核对文章数字（不信任内部一致性），并检查 YMYL 语气、AI 写作痕迹、格式硬指标、跨站边界。发现并修复：
  1. **`how-to-become-a-phlebotomist` 编造引语**（FAIL 级）：正文把 BLS 官网两句话的内容拼接成一句又加了引号冒充逐字引用，实际 BLS 原文并无这句话。已改写为不带引号的转述，并额外补上被同批发现的 FAQ 免责声明缺失（跟同类"how to become"文章保持一致）
  2. `what-does-a-physician-assistant-do`：正文/FAQ 都把 PA 专科三件套写成"primary care, emergency medicine, and surgery"，但 BLS 官网原文是"primary care and family medicine, emergency medicine, and psychiatry"（surgery 只是页面里举例提到的其他专科之一，不在官方三件套里）。已订正，并顺带把该职业缺失的百分位数据（BLS 页面有但 `wages-source.json` 里留空）补齐（p10 $95,240 / p90 $182,200，独立二次 WebFetch 确认）
  3. `what-does-an-actuary-do` FAQ：把"SOA/CAS 考试体系"的机构名归因成"根据 BLS 官网"，但 BLS 原文只说"a related certification program"没有点名这两个机构（SOA/CAS 命名本身是真实、可独立验证的行业常识，只是不该算在 BLS 头上）。已改写归因表述
  4. `what-does-a-paralegal-do`：正文把"雇主愿意招收无正式 paralegal 教育背景的人并给在职培训"和"招收有本科学位的人给在职培训"这两类不同人群的表述混在一句里。已拆开澄清
  5. `actuary-salary` / `pharmacist-salary`：各有一处可信但未标来源的事实性论断（actuary 按行业的雇佣人数排名、pharmacist 关于零售药房工作条件的媒体报道），BLS 引用页面本身不支持这类具体断言。已删除或改写为不依赖该断言的表述
  6. **全站 em dash 过度使用**：审核 agent 指出多篇重复出现"X — Y（同位语说明）— Z"结构，是可辨识的 AI 写作痕迹。用 `humanizer` skill 的规则（em dash 一律替换成句号/逗号/冒号/括号）逐条改写 `src/data/guides.ts`，改完 `grep -c '—\|–' src/data/guides.ts` 确认为 0
- **构建验证**：修复后重新 `npm test`（10 项全绿）+ `npm run build`（18 页全部生成，0 错误）。
- **部署**：GitHub 仓库 `zhangwenhao66/wagelark`（public，与 dayalmanac/calcbadger/umberlore 一致），Cloudflare Pages 项目 `wagelark` 通过 Git 集成自动部署（首次 push 后 CF 自动触发构建，29 秒完成），自定义域名 `wagelark.com` 绑定后立即生效（同账号内 DNS + Pages，无需等待 DNS 传播）。`curl` 实测：首页 200、10 篇文章页 200、不存在路径返回真实 404（`<title>Page Not Found | WageLark</title>`）。
- **基础设施接入**（建站计划第十一节六项）：
  1. `独立站/流量站清单.md` 状态改"运营中"
  2. AdSense：加 site + AdSense code snippet 验证通过 + ads.txt 已在线 + 已提交 Request review（状态"Getting ready"，跟其余五站一致，随 8/8 `adsense-review-followup` 一并复查）
  3. contact@wagelark.com：Cloudflare Email Routing（收信）已配置生效；Gmail「Send mail as」（发信，走 Mailjet SMTP 587/TLS）**未完成**——添加向导会打开一个真实浏览器弹窗，本环境 Chrome 自动化的点击虽然能触发对应 JS handler，但产生的 `window.open()` 被判定为非可信手势而遭浏览器弹窗拦截（`window.open()` 直接测试返回 `false` 确认），需要 Owen 手动完成这一步（跟其余站建站时大概率遇到的是同一个环境限制）。`gmail_send.py` 的 `FROM_ALIASES` 暂不加 wagelark 条目，按脚本自身注释的规则，要等这一步验证通过、实测发信读到达信 From 头确认后再加
  4. GSC：Domain property 通过 Cloudflare DNS TXT 记录验证；`sitemap-index.xml` 已提交
  5. IndexNow：密钥文件 `public/298c76621a703c1a962fc335ca832196.txt` 已部署；首批 10 篇 URL 已提交（Bing 202 / Yandex 202），记录见 `indexnow-submit-log.json`
  6. 候选 subreddit（r/careerguidance、r/jobs、各行业专业 sub）已写入 `独立站/流量站清单.md` 对应行，供 `trafficsite-backlink-building` 起点参考
- **定时任务**：`wagelark-content-publishing` 已创建并启用（cron `30 21 * * *`，即每天 21:30），sonnet5，1 篇/天起步，SKILL.md 以 mythcairn 模板为骨架替换了州页红线/BLS 数据纪律/YMYL 边界/年度刷新/跨站边界五个专属章节，并把本次审核实际抓到的问题模式（编造引语、机构名误归因、em dash 过密）写成了具体的检查提示。
- **累计上线**：10 篇（Salary Guide 4、Career Guide 3、How to Become 2、口语化查询 1）
- **下一步**：等 2026-08-10 后州组合页解禁，届时每篇仍需满足纪律三的 4 项该州独有内容；Gmail Send-As 手动步骤完成后记得同步更新本文件和 `流量站清单.md` 里的"待Owen完成"标注。
