# WageLark 工资分位数工具 —— 实施方案

**背景**：2026-08-04 调研发现 Google AI Overview 对"单一事实查询"型页面（比如现在每个职业页只展示一个薪资中位数）点击率杀伤力最大，对"需要用户输入多个变量才能算出个性化结果"的工具型页面杀伤力最小（AI 讲得清公式但没法替用户执行）。WageLark 现在的 `/salary-guides/[slug]` 页面正好是最危险的单一数字格式，这个工具是把它改造成真正的多输入个性化工具。

**原计划是"地区+经验年限"个性化计算器，已经否决**：`src/data/bls-wages.ts` 里没有任何分地区、分经验年限的细分数据，做那种计算器等于编造 BLS 没发布过的数字，违反项目"绝不编造资历/数据"的硬规则。

**现方案：工资分位数定位工具**。数据 100% 真实，来自已有的 `percentiles: {p10, p25, p75, p90}` 字段。

## 功能描述

用户在某个职业页面上，输入"你自己现在的年薪"，工具用该职业已有的 `medianAnnual` + `percentiles` 数据，通过分段线性插值算出这个数字落在该职业薪资分布的大约第几分位（比如"你的收入超过了该职业约62%的从业者"），并且：

- 如果该职业有 `industryWages`（行业细分薪资），额外展示"换到 XX 行业，同职位薪资中位数是 $Y"的对比（不是个性化计算，是已有真实数据的展示，说清楚这是行业中位数不是给这个用户的预测）
- 结果里注明数据来源和年份（`sourceLabel` / `dataYear`），跟站内其他内容一致

## 数据与算法

数据源：`src/data/bls-wages.ts` 的 `BlsWageEntry`，每条职业已有：
```
medianAnnual: number
percentiles: { p10?, p25?, p75?, p90? }  // 部分职业字段可能缺失，需处理 undefined
```

分位数插值逻辑（纯函数，无副作用，需要单元测试）：
- 已知点：(10, p10), (25, p25), (50, medianAnnual), (75, p75), (90, p90) —— 过滤掉 undefined 的点
- 用户输入值落在哪两个已知点之间，做线性插值算出对应百分位
- 低于最小已知点：明确说"低于该职业统计到的第10百分位"，不做外推超过0
- 高于最大已知点：明确说"高于该职业统计到的第90百分位"，不做外推超过100
- 如果某条目 percentiles 全部缺失（只有 medianAnnual），工具应优雅降级：只能说"高于/低于/约等于中位数"，不能算精确分位

## 实施位置（参照 CalcBadger 模式，但不新增框架依赖）

WageLark 目前是纯静态 Astro，没有装 `@astrojs/preact`/React。**不要新增前端框架依赖**——用一个 vanilla `<script type="module">` island 即可，跟 CalcBadger 的"纯逻辑函数 + 薄UI包装"思路一致，只是不用 Preact：

1. `src/lib/salaryPercentile.ts` —— 纯计算函数 `estimatePercentile(input: number, entry: BlsWageEntry): PercentileResult`，附带单元测试 `salaryPercentile.test.ts`（用 vitest，跟本仓库其他单测一致的框架）。测试用例至少覆盖：五个已知点都存在、部分 percentile 缺失、输入低于p10、输入高于p90、输入正好等于某个已知点。
2. 在 `src/pages/salary-guides/[slug].astro` 页面里加一个新组件区块（比如 `src/components/SalaryPercentileTool.astro`），包含一个数字输入框 + 按钮，用内联 `<script>` 调用 `salaryPercentile.ts` 里的函数（客户端计算，不需要请求后端），结果实时渲染到页面上。
3. 组件需要把该职业的 `percentiles`/`medianAnnual`/`industryWages` 作为 props 传入（构建时静态注入，不是运行时请求 API）。

## 验收标准

- `npm test` 全绿，新增测试覆盖上述边界情况
- `npm run build` 无错误
- 手动验证至少 2 个职业页面（一个 percentiles 齐全的、一个有缺失字段的）功能正常
- 工具本身不需要过内容审核七重检查（这是产品功能不是文章内容），但如果顺手改了页面文案要过一下 `Skill(humanizer)`
- 完成后 `git commit` + `push`，Cloudflare Pages 自动部署，轮询新页面确认线上生效
- 在仓库根 `内容发布日志.md` 或类似日志追加一条记录说明这是产品功能上线，不是常规文章
